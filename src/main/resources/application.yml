spring:
  application:
    name: oaka
server:
  port: 8082

---

spring:
  config:
    activate:
      on-profile: local
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

---

spring:
  config:
    activate:
      on-profile: test
  cloud:
    discovery:
      enabled: true

---

spring:
  config:
    activate:
      on-profile: mysql
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://172.16.0.7:3310/money-maker-test
    username: mm-app
    password: SaveMoney
    driver-class-name: com.mysql.cj.jdbc.Driver
    data:
